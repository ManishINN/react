According to this documentation it is possible and works together with an onSubmit function that you need to implement in your React class.

To save your field contents, you can add them to the component state, e.g. onBlur. The handleBlur function would call this.setState to add the information to the component state, and via this.state you can pull it out and insert it into the field in your render method.